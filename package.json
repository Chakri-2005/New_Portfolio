{
  "name": "portfolio-root",
  "private": true,
  "scripts": {
    "install": "npm install --prefix frontend",
    "build": "npm run build --prefix frontend"
  },
  "dependencies": {
    "nodemailer": "^6.9.13"
  },
  "devDependencies": {
    "@types/nodemailer": "^6.4.14",
    "@vercel/node": "^3.0.28"
  }
}
